name: agent_quickstart_flutter
description: "Flutter quickstart for building Agora Conversational AI experiences."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  agora_rtc_engine: ^6.6.3
  agora_rtm: ^2.2.0
  http: ^1.6.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
