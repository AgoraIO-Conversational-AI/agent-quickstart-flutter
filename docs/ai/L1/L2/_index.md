# L2 Deep Dives

Use these only when a task needs more detail than L1 provides.

- Add a new deep dive file when a workflow becomes complex enough to deserve its own page.
- Keep the L2 list short so agents do not spend extra context on unused detail.

